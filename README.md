# Directories

`src/` - contains sql commands utilized for different policy configurations, tpch db loader, test user creation  
`executed_plans/` - contains images of plans actually executed for different policy configurations at run time with execution time, number of buffer accesses, physical reads for each operations involved in plan
